import React from "react";

import ChangePassword from "@/shared/change-password";

const CurrentChangePassword: React.FC = () => {
  return (
    <ChangePassword variant="current" />
  );
};

export default CurrentChangePassword;
