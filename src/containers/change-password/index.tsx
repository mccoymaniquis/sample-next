import React from "react";

import ChangePassword from "@/shared/change-password";

const DefaultChangePassword: React.FC = () => {
  return (
    <ChangePassword variant="default" />
  );
};

export default DefaultChangePassword;
